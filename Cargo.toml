[package]
name = "bloodthief_auto_splitter"
description = "An autosplitter for Bloodthief written in rust"
version = "0.1.0"
edition = "2021"

[dependencies]
asr = { git = "https://github.com/livesplit/asr.git", version = "0.1.0", features = ["godot", "signature"] }

[lib]
crate-type = ["cdylib"]
